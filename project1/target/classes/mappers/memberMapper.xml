<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.mapper.MemberMapper">
	<!-- namespace를 사용해서 mapper 파일을 찾아오는 주소 설정 -->
	
	<!-- 시간정보 조회 -->
	<select id="getTime" resultType="string">
		select now()
	</select>
	
	<!-- 로그인 체크 -->
	<select id="loginMember" resultType="com.project.domain.MemberVO">
	   select * from employee
	   where id=#{id} and pw=#{pw}	
	</select>
	
	
	<!-- 회원정보 조회 -->
	<select id="getMember" resultType="MemberVO">
		select * from employee
		 where userid=#{userid}	
	</select>
	
	
	<!-- 회원정보 수정 -->
	<update id="updateMember">
		update employee set username=#{username}, useremail=#{useremail}, updatedate=now()  
		where userid=#{userid} and userpw=#{userpw}	
	</update>
	
	<!-- 회원정보 삭제 -->
	<delete id="deleteMember">
		delete from employee
		 where userid = #{userid} and userpw = #{userpw}	
	</delete>	
	
	<!-- 회원정보 목록(list) -->
	<select id="getMemberList" resultType="MemberVO">
		select * from employee
	</select>
	
	
	
	
	
	
	
	
	
	
	
	

</mapper>  
  